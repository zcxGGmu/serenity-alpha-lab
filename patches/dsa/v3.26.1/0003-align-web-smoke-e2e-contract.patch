diff --git a/apps/dsa-web/e2e/report-markdown.spec.ts b/apps/dsa-web/e2e/report-markdown.spec.ts
index b5c0276..9509c24 100644
--- a/apps/dsa-web/e2e/report-markdown.spec.ts
+++ b/apps/dsa-web/e2e/report-markdown.spec.ts
@@ -26,6 +26,10 @@ async function login(page: Page) {

   // Fill password and submit
   await page.locator('#password').fill(smokePassword!);
+  const passwordConfirmInput = page.locator('#passwordConfirm');
+  if (await passwordConfirmInput.isVisible({ timeout: 500 }).catch(() => false)) {
+    await passwordConfirmInput.fill(smokePassword!);
+  }

   // Wait for and click the submit button
   const submitButton = page.getByRole('button', { name: /授权进入工作台|完成设置并登录/ });
@@ -47,33 +51,42 @@ async function login(page: Page) {
   await expect(stockInput).toBeVisible({ timeout: 10_000 });
 }

+async function openFirstHistoryReport(page: Page) {
+  await page.goto('/');
+  await page.waitForLoadState('domcontentloaded');
+
+  const existingReportButton = page.getByRole('button', { name: '完整分析报告' });
+  const hasExistingReport = await expect(existingReportButton)
+    .toBeVisible({ timeout: 5000 })
+    .then(() => true)
+    .catch(() => false);
+  if (hasExistingReport) {
+    await existingReportButton.click();
+    await expect(page.getByRole('dialog').getByText('完整分析报告')).toBeVisible();
+    return;
+  }
+
+  await expect(page.getByRole('heading', { name: '个股栏' })).toBeVisible({ timeout: 10_000 });
+
+  const firstHistoryItem = page.locator('.home-history-item').first();
+  await expect(firstHistoryItem).toBeVisible({ timeout: 10_000 });
+  await firstHistoryItem.click();
+
+  const detailedReportButton = page.getByRole('button', { name: '完整分析报告' });
+  await expect(detailedReportButton).toBeEnabled({ timeout: 5000 });
+  await expect(detailedReportButton).toBeVisible({ timeout: 5000 });
+  await detailedReportButton.click();
+
+  await expect(page.getByRole('dialog').getByText('完整分析报告')).toBeVisible();
+}
+
 test.describe('ReportMarkdown component', () => {
   test('copy markdown source code', async ({ page, context }) => {
     // Grant clipboard permissions
     await context.grantPermissions(['clipboard-read', 'clipboard-write']);

     await login(page);
-
-    // Navigate to history page
-    await page.getByRole('link', { name: '首页' }).click();
-    await page.waitForLoadState('domcontentloaded');
-    // Wait for history panel to load
-    await expect(page.getByText('历史分析')).toBeVisible({ timeout: 10_000 });
-
-    // Click on the first history item to select it
-    const firstHistoryItem = page.locator('.home-history-item').first();
-    await expect(firstHistoryItem).toBeVisible({ timeout: 10_000 });
-    await firstHistoryItem.click();
-    // Wait for detailed report button to be enabled (indicates selection is complete)
-    const detailedReportButton = page.getByRole('button', { name: '完整分析报告' });
-    await expect(detailedReportButton).toBeEnabled({ timeout: 3000 });
-
-    // Click the "完整分析报告" button to open the markdown drawer
-    await expect(detailedReportButton).toBeVisible({ timeout: 5000 });
-    await detailedReportButton.click();
-
-    // Verify drawer content is visible
-    await expect(page.getByRole('dialog').getByText('完整分析报告')).toBeVisible();
+    await openFirstHistoryReport(page);

     // Click copy markdown button
     const copyMarkdownButton = page.getByRole('button', { name: '复制 Markdown 源码' });
@@ -98,27 +111,7 @@ test.describe('ReportMarkdown component', () => {
     await context.grantPermissions(['clipboard-read', 'clipboard-write']);

     await login(page);
-
-    // Navigate to history page
-    await page.getByRole('link', { name: '首页' }).click();
-    await page.waitForLoadState('domcontentloaded');
-    // Wait for history panel to load
-    await expect(page.getByText('历史分析')).toBeVisible({ timeout: 10_000 });
-
-    // Click on the first history item to select it
-    const firstHistoryItem = page.locator('.home-history-item').first();
-    await expect(firstHistoryItem).toBeVisible({ timeout: 10_000 });
-    await firstHistoryItem.click();
-    // Wait for detailed report button to be enabled (indicates selection is complete)
-    const detailedReportButton = page.getByRole('button', { name: '完整分析报告' });
-    await expect(detailedReportButton).toBeEnabled({ timeout: 3000 });
-
-    // Click the "完整分析报告" button to open the markdown drawer
-    await expect(detailedReportButton).toBeVisible({ timeout: 5000 });
-    await detailedReportButton.click();
-
-    // Verify drawer content is visible
-    await expect(page.getByRole('dialog').getByText('完整分析报告')).toBeVisible();
+    await openFirstHistoryReport(page);

     // Click copy plain text button
     const copyPlainTextButton = page.getByRole('button', { name: '复制纯文本' });
@@ -157,14 +150,7 @@ test.describe('ReportMarkdown component', () => {
     // On mobile, a report should already be selected (showing in main content)
     // Wait for main content to load
     await expect(page.getByPlaceholder('输入股票代码或名称，如 600519、贵州茅台、AAPL')).toBeVisible({ timeout: 10_000 });
-
-    // Click the "完整分析报告" button to open the markdown drawer
-    const detailedReportButton = page.getByRole('button', { name: '完整分析报告' });
-    await expect(detailedReportButton).toBeVisible({ timeout: 5000 });
-    await detailedReportButton.click();
-
-    // Verify drawer content is visible (this ensures drawer is fully open)
-    await expect(page.getByRole('dialog').getByText('完整分析报告')).toBeVisible({ timeout: 10000 });
+    await openFirstHistoryReport(page);

     // Verify toolbar buttons are visible and clickable on mobile
     const copyMarkdownButton = page.getByRole('button', { name: '复制 Markdown 源码' });
@@ -180,24 +166,7 @@ test.describe('ReportMarkdown component', () => {

   test('buttons are disabled during loading', async ({ page }) => {
     await login(page);
-
-    // Navigate to history page
-    await page.getByRole('link', { name: '首页' }).click();
-    await page.waitForLoadState('domcontentloaded');
-    // Wait for history panel to load
-    await expect(page.getByText('历史分析')).toBeVisible({ timeout: 10_000 });
-
-    // Click on the first history item to select it
-    const firstHistoryItem = page.locator('.home-history-item').first();
-    await expect(firstHistoryItem).toBeVisible({ timeout: 10_000 });
-    await firstHistoryItem.click();
-    // Wait for detailed report button to be enabled (indicates selection is complete)
-    const detailedReportButton = page.getByRole('button', { name: '完整分析报告' });
-    await expect(detailedReportButton).toBeEnabled({ timeout: 3000 });
-
-    // Click the "完整分析报告" button to open the markdown drawer
-    await expect(detailedReportButton).toBeVisible({ timeout: 5000 });
-    await detailedReportButton.click();
+    await openFirstHistoryReport(page);

     // Immediately check if buttons are disabled (right after drawer opens)
     const copyMarkdownButton = page.getByRole('button', { name: '复制 Markdown 源码' });
diff --git a/apps/dsa-web/e2e/smoke.spec.ts b/apps/dsa-web/e2e/smoke.spec.ts
index ae1864d..e9bccf1 100644
--- a/apps/dsa-web/e2e/smoke.spec.ts
+++ b/apps/dsa-web/e2e/smoke.spec.ts
@@ -40,6 +40,10 @@ async function login(page: Page) {

   await expect(passwordInput).toBeVisible({ timeout: 10_000 });
   await passwordInput.fill(smokePassword!);
+  const passwordConfirmInput = page.locator('#passwordConfirm');
+  if (await passwordConfirmInput.isVisible({ timeout: 500 }).catch(() => false)) {
+    await passwordConfirmInput.fill(smokePassword!);
+  }
   await expect(submitButton).toBeVisible();

   await Promise.all([
@@ -82,7 +86,10 @@ test.describe('web smoke', () => {
     await expect(stockInput).toBeVisible({ timeout: 10_000 });
     await expect(page.getByRole('link', { name: '首页' })).toBeVisible();
     await expect(page.getByRole('link', { name: '问股' })).toBeVisible();
-    await expect(page.getByText('历史分析')).toBeVisible();
+    await expect(page.getByRole('heading', { name: '个股栏' })).toBeVisible();
+    await expect(page.getByRole('button', { name: '历史', exact: true })).toBeVisible();
+    await expect(page.getByRole('button', { name: '自选', exact: true })).toBeVisible();
+    await expect(page.getByRole('button', { name: '今日', exact: true })).toBeVisible();

     await stockInput.fill('600519');
     const analyzeButton = page.getByRole('button', { name: '分析', exact: true });
@@ -105,7 +112,7 @@ test.describe('web smoke', () => {

     const input = page.getByPlaceholder(/分析 600519/);
     await expect(input).toBeVisible({ timeout: 5000 });
-    await expect(page.getByText('策略', { exact: true })).toBeVisible();
+    await expect(page.getByTestId('chat-skill-picker-panel').getByText('策略', { exact: true })).toBeVisible();

     const prompt = '请简要分析 600519';
     await input.fill(prompt);
@@ -193,8 +200,9 @@ test.describe('web smoke', () => {
     await page.waitForTimeout(1000);

     await expect(page.getByRole('heading', { name: 'System settings' })).toBeVisible({ timeout: 10_000 });
-    await expect(page.getByRole('button', { name: 'Send test' })).toBeVisible();
-    await expect(page.getByRole('textbox', { name: 'Title' })).toHaveValue('DSA notification test');
+    await expect(page.getByRole('button', { name: 'Reset' })).toBeVisible();
+    await expect(page.getByRole('button', { name: /Save configuration/ })).toBeVisible();
+    await expect(page.getByText('First-run setup check')).toBeVisible();

     await captureSmokeScreenshot(page, testInfo, 'smoke-settings-page-en');
   });
