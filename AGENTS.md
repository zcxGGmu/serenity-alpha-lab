# Serenity Alpha Lab 协作约定

## 阶段提交纪律

- 以 `docs/development-progress-checklist.md` 为开发执行账本。
- 每完成一个阶段 Gate（`G0` 至 `G6`），必须在同一工作会话内完成：更新清单状态和验收证据、运行相应校验、提交一个中文 Git checkpoint commit。
- 阶段内如果一个任务或一组紧密相关任务已经形成可运行、可评审、可回滚的交付，也应单独提交；不要把多个无关模块混在一个 commit。
- 任何未通过 Gate 的工作不得标为完成或伪造提交；阻塞项必须记录在清单中。

## 提交前检查

- 先执行与改动匹配的测试、静态检查或文档结构校验。
- 检查 `git status`，只暂存本次交付相关文件；不覆盖或回退用户已有改动。
- 更新方案、任务清单、ADR、Runbook、许可证或证据登记中受影响的部分。
- 提交信息使用中文，标题说明阶段或能力，正文至少说明：完成内容、兼容性/风险处理、验证方式和关联任务 ID。

## 提交信息模板

```text
<类型>(<范围>): <中文摘要>

完成内容：
- ...

兼容性与风险：
- ...

验证：
- ...

关联任务：SAL-Px-NNN, Gate Gy
```

常用类型：`docs`、`feat`、`fix`、`refactor`、`test`、`build`、`chore`。

## 会话恢复

- 新会话开始时先阅读本文件、开发方案和进度清单，再确认当前 Phase、Gate、阻塞项和未提交改动。
- 阶段完成时主动执行提交，不等待用户再次提醒；若用户明确要求不提交，则以用户要求为准。
