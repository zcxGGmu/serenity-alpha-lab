# Python 依赖 Extras 与锁文件记录

> Task: `SAL-P1-003` 建立依赖 Extras 与锁文件<br>
> Date: 2026-07-20<br>
> Baseline: DSA `v3.26.1 @ e8a9ca7742e8cb2498c8f491dd76d239b3064e1a`<br>
> Scope: Serenity root Python dependency surfaces, lock file, and requirements drift guard only.

## Summary

Serenity root Python dependencies now use explicit install surfaces:

| Surface | Purpose |
|---|---|
| `core` | API, metadata, report rendering, LLM routing, data processing primitives, and DSA compatibility runtime basics. |
| `providers` | Market/news/provider SDKs and API clients used by the inherited DSA provider chain. |
| `desktop` | Bot/notification and local delivery SDKs needed by desktop or standalone user workflows. |
| `quant` | Optional future quant stack (`polars`, `pyarrow`, `duckdb`, `pyqlib`) that is not installed by desktop/production requirements. |
| `dev` | Test, lint, format, coverage, and security tooling. |

`uv.lock` is the authoritative lock file. Root `requirements.txt` is a generated compatibility export for `core + providers + desktop`; it intentionally excludes `quant` and `dev`.

## AlphaSift Handling

The DSA baseline uses `git+https://github.com/ZhuLinsen/alphasift.git@9f522747caafd3c0b1ddb7e14d5cf44c8580b6cf`. Serenity no longer declares that dynamic Git install in root `pyproject.toml` or generated production `requirements.txt`.

P0 evidence already proved the cached wheel shape as `alphasift-0.2.0-py3-none-any.whl` with SHA-256 `c9890b127ad062253200bf4b7fe5816bff42a2e95e69ee04b985542cd9e39cf2`. That wheel remains a cache artifact, not a committed dependency. The later AlphaSift adapter task must approve the package/wheel source, hash, SBOM, and license intake before adding AlphaSift to a production install surface.

## Generated Outputs

| Output | Treatment |
|---|---|
| `uv.lock` | Checked in; generated by `uv lock`; includes all root extras for reproducible resolution. |
| `requirements.txt` | Checked in; generated by `scripts/verify-python-dependency-lock.sh`; excludes project editable install, dev tools, and `quant`. |
| `scripts/verify-python-dependency-lock.sh` | CI-compatible drift guard; runs `uv lock --check`, re-exports the production requirements surface, and `diff -u`s it against the committed file. |

## Verification

- Red tests first failed on missing extras, dynamic Git dependency, missing `uv.lock`, missing `requirements.txt`, missing drift guard, and missing Run lifecycle model.
- Green verification: `scripts/verify-python-dependency-lock.sh` passed.
- `tests/architecture/test_dependency_locking.py` checks the expected extras, rejects direct Git dependency declarations, and confirms production requirements exclude `pyqlib` and AlphaSift Git URLs.
